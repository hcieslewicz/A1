# -*- coding: utf8 -*-


def test_cases(number):
    return testCases[number]


testCases = [
    # [severity, description]
    ['Blocker', 'When user goes to Search page, page should be loaded'],
    ['Blocker', 'On search page, when user filter by first registration and sort by proce descending, he should see '
                'results filtered by first registration amnd sorted by price descending'],
]
